

int main(int argc, char *argv[])
{

//    QApplication a(argc, argv)
//    QSurfaceFormat format;
//    format.setDepthBufferSize(24);
//    format.setStencilBufferSize(8);
//    format.setVersion(1, 0);
//    format.setProfile(QSurfaceFormat::CoreProfile);

//    auto renderWidget = std::make_shared<QtRenderWidget>(nullptr);
//    renderWidget->setFormat(format);
//    renderWidget->show();

//    auto graphicsRenderer = renderWidget->renderer();
//    auto audioRenderer = std::make_shared<OpenALRenderer>();
//    auto physicsRenderer = std::make_shared<ODERenderer>();

//    auto testApplication = std::make_shared<TestApplication>(graphicsRenderer, audioRenderer, physicsRenderer);
//    renderWidget->setApplication(testApplication);

//    return QApplication::exec();
    return 0;
}
